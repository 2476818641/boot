# Plus 版插件配置：在纯净版基础上增加常用服务
# 注意：本文件会在 GENERAL_AX6600.txt 之后追加，必要时覆盖纯净版的 n 选项。

# Docker / 容器：本 fork 去掉（用不到，且整套容器栈占闪存与内存）
# 下面显式写 =n 而不是删行：一是留痕说明"是故意不要"，二是万一有别的包反向依赖，defconfig 会报出来
CONFIG_PACKAGE_luci-app-dockerman=n
CONFIG_PACKAGE_dockerd=n
CONFIG_PACKAGE_docker=n
CONFIG_PACKAGE_docker-compose=n
CONFIG_PACKAGE_luci-lib-docker=n
CONFIG_PACKAGE_cgroupfs-mount=n
CONFIG_PACKAGE_tini=n
CONFIG_PACKAGE_kmod-veth=n
CONFIG_PACKAGE_kmod-ikconfig=y
CONFIG_PACKAGE_kmod-nf-ipvs=n	# 只服务于容器/负载均衡，跟着 docker 一起去掉
CONFIG_PACKAGE_kmod-nf-nat=y
CONFIG_PACKAGE_kmod-nf-nat6=y
CONFIG_PACKAGE_kmod-nft-nat=y
CONFIG_PACKAGE_kmod-nft-offload=y
CONFIG_PACKAGE_kmod-nft-compat=y
CONFIG_PACKAGE_kmod-nft-fullcone=y

# PassWall2：本 fork 已移除（与 OpenClash 功能重叠，用户只用 OpenClash）。
# 显式写 =n 而不是删行：一是留痕"故意不要"，二是万一有别的包反向依赖，make defconfig 会暴露出来。
CONFIG_PACKAGE_luci-app-passwall2=n
CONFIG_PACKAGE_luci-app-passwall2_Basic_Core_All=n
CONFIG_PACKAGE_luci-app-passwall2_Iptables_Transparent_Proxy=n
CONFIG_PACKAGE_luci-app-passwall2_Nftables_Transparent_Proxy=n
CONFIG_PACKAGE_luci-app-passwall2_INCLUDE_Haproxy=n
CONFIG_PACKAGE_luci-app-passwall2_INCLUDE_Hysteria=n
CONFIG_PACKAGE_luci-app-passwall2_INCLUDE_NaiveProxy=n
CONFIG_PACKAGE_luci-app-passwall2_INCLUDE_Shadowsocks_Rust_Client=n
CONFIG_PACKAGE_luci-app-passwall2_INCLUDE_ShadowsocksR_Libev_Client=n
CONFIG_PACKAGE_luci-app-passwall2_INCLUDE_Simple_Obfs=n
CONFIG_PACKAGE_luci-app-passwall2_INCLUDE_V2ray_Plugin=n
# 下面三个原本是为 PW2 显式加的（PW2 不自动引入）：OpenClash 自己会下载 geo 数据，所以一并去掉。
# 想恢复：改成 =y，并同时恢复 .github/workflows/WRT-CORE.yml 里 passwall_packages 那行 feed
#（v2ray-geoip / v2ray-geosite / geoview 由那个 feed 提供）。
CONFIG_PACKAGE_v2ray-geoip=n
CONFIG_PACKAGE_v2ray-geosite=n
CONFIG_PACKAGE_geoview=n

# PassWall 一代（已由 PassWall2 取代，fork 者如需可取消注释，
# 并同步取消 Scripts/Packages.sh 中对应的克隆行）
#CONFIG_PACKAGE_luci-app-passwall=y
#CONFIG_PACKAGE_luci-app-passwall_Iptables_Transparent_Proxy=n
#CONFIG_PACKAGE_luci-app-passwall_Nftables_Transparent_Proxy=y
#CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Geoview=y
#CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Haproxy=y
#CONFIG_PACKAGE_luci-app-passwall_INCLUDE_NaiveProxy=y
#CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Shadowsocks_Rust_Client=y
#CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Shadowsocks_Libev_Client=y
#CONFIG_PACKAGE_luci-app-passwall_INCLUDE_ShadowsocksR_Libev_Client=y
#CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Simple_Obfs=y
#CONFIG_PACKAGE_luci-app-passwall_INCLUDE_SingBox=y
#CONFIG_PACKAGE_luci-app-passwall_INCLUDE_V2ray_Geodata=y
#CONFIG_PACKAGE_luci-app-passwall_INCLUDE_V2ray_Plugin=y
#CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Xray=y
#CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Xray_Plugin=y
#CONFIG_PACKAGE_luci-app-passwall_INCLUDE_Trojan_Plus=y

# OpenClash
CONFIG_PACKAGE_luci-app-openclash=y
CONFIG_PACKAGE_luci-compat=y
# OpenClash 及 nftset 依赖 dnsmasq-full，显式声明避免上游默认值变化时被 defconfig 静默丢弃
CONFIG_PACKAGE_dnsmasq-full=y
CONFIG_PACKAGE_dnsmasq=n
CONFIG_PACKAGE_dnsmasq_full_nftset=y
CONFIG_PACKAGE_dnsmasq_full_ipset=n

# AdGuard Home
CONFIG_PACKAGE_luci-app-adguardhome=y
CONFIG_PACKAGE_adguardhome=y

# 实用工具（对应源码由 Packages.sh 在 PLUS 版克隆）
CONFIG_PACKAGE_luci-app-partexp=y
CONFIG_PACKAGE_luci-app-wolplus=y

# 实用工具（来自官方 feeds；samba4 文件共享已在通用配置 GENERAL_AX6600.txt 中启用）
# 终端（网页 SSH）
CONFIG_PACKAGE_luci-app-ttyd=y
# UPnP 自动端口转发（通用配置中为 n，此处覆盖，仅 PLUS 启用）
CONFIG_PACKAGE_luci-app-upnp=y
# 动态域名解析（服务商定义是独立子包，不加则界面服务商列表为空）
CONFIG_PACKAGE_luci-app-ddns=y
CONFIG_PACKAGE_ddns-scripts-services=y
CONFIG_PACKAGE_ddns-scripts-aliyun=y
CONFIG_PACKAGE_ddns-scripts-dnspod=y
CONFIG_PACKAGE_ddns-scripts-cloudflare=y

# WireGuard 用户态工具与 LuCI 协议支持（issue #15 用户请求；
# 驱动 kmod-wireguard 已在通用配置 GENERAL_AX6600.txt 中启用，此处补齐配置与管理所需部分）
CONFIG_PACKAGE_wireguard-tools=y
CONFIG_PACKAGE_luci-proto-wireguard=y

# USB 打印机支持（issue #11 用户请求；滚动内核无法事后 opkg 装 kmod，须随固件集成）
CONFIG_PACKAGE_kmod-usb-printer=y
# p910nd 打印服务器及其管理界面
CONFIG_PACKAGE_p910nd=y
CONFIG_PACKAGE_luci-app-p910nd=y
